# Programa: p022-resistencia-equivalente-paralelo.py
# Calcular la resistencia equivalente de tres resistencias en paralelo

# Entrada
R1 = float(input("Ingresa el valor de la resistencia R1 (ohms): "))
R2 = float(input("Ingresa el valor de la resistencia R2 (ohms): "))
R3 = float(input("Ingresa el valor de la resistencia R3 (ohms): "))

# Proceso
Req = 1 / (1/R1 + 1/R2 + 1/R3)

# Salida
print(f"La resistencia equivalente en paralelo es: {Req:.2f} ohms")

